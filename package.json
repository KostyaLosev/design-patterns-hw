{
  "name": "patterns-proxy-command",
  "version": "1.0.0",
  "description": "Proxy + Command pattern example for secure file access",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc --noEmit"
  },
  "keywords": [
    "patterns",
    "proxy",
    "command",
    "typescript"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.13.10",
    "typescript": "^5.8.2"
  }
}
